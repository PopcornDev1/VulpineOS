diff --git a/netwerk/protocol/http/nsHttpChannel.cpp b/netwerk/protocol/http/nsHttpChannel.cpp
--- a/netwerk/protocol/http/nsHttpChannel.cpp
+++ b/netwerk/protocol/http/nsHttpChannel.cpp
@@ -7772,6 +7761,11 @@ nsresult nsHttpChannel::BeginConnect() {
         .Add();
   }
 
+  // VulpineOS: isolate connection pool per browser context
+  if (mConnectionInfo) {
+    mConnectionInfo->SetOriginContextId(mLoadInfo->GetInnerWindowID());
+  }
+
   bool trrEnabled = false;
   auto dnsStrategy = GetProxyDNSStrategy();
   bool httpsRRAllowed =
diff --git a/netwerk/protocol/http/nsHttpConnectionInfo.cpp b/netwerk/protocol/http/nsHttpConnectionInfo.cpp
index 13d386eecb..b35a9fafa2 100644
--- a/netwerk/protocol/http/nsHttpConnectionInfo.cpp
+++ b/netwerk/protocol/http/nsHttpConnectionInfo.cpp
@@ -280,11 +280,20 @@ void nsHttpConnectionInfo::BuildHashKey() {
     mHashKey.AppendLiteral("}");
   }
 
+  // VulpineOS: partition connection pool per browser context
+  mHashKey.AppendLiteral("$ctx=");
+  mHashKey.AppendInt(static_cast<int64_t>(mOriginContextId));
+
   nsAutoCString originAttributes;
   mOriginAttributes.CreateSuffix(originAttributes);
   mHashKey.Append(originAttributes);
 }
 
+void nsHttpConnectionInfo::SetOriginContextId(uint64_t aOriginContextId) {
+  mOriginContextId = aOriginContextId;
+  RebuildHashKey();
+}
+
 void nsHttpConnectionInfo::RebuildHashKey() {
   // Create copies of all properties stored in our hash key.
   bool isAnonymous = GetAnonymous();
@@ -350,6 +359,7 @@ already_AddRefed<nsHttpConnectionInfo> nsHttpConnectionInfo::Clone() const {
   clone->SetHasIPHintAddress(HasIPHintAddress());
   clone->SetEchConfig(GetEchConfig());
   clone->SetWebTransportId(GetWebTransportId());
+  clone->SetOriginContextId(mOriginContextId);
   MOZ_ASSERT(clone->Equals(this));
 
   return clone.forget();
@@ -637,6 +647,9 @@ void nsHttpConnectionInfo::BuildOriginFrameHashKey(nsACString& newKey,
   ci->GetOriginAttributes().CreateSuffix(suffix);
   newKey.Append(suffix);
   newKey.AppendLiteral("]viaORIGIN.FRAME");
+  // VulpineOS: partition coalescing by context ID
+  newKey.AppendLiteral("$ctx=");
+  newKey.AppendInt(static_cast<int64_t>(ci->OriginContextId()));
 }
 
 }  // namespace net
diff --git a/netwerk/protocol/http/nsHttpConnectionInfo.h b/netwerk/protocol/http/nsHttpConnectionInfo.h
index 920267460e..ee30abf65f 100644
--- a/netwerk/protocol/http/nsHttpConnectionInfo.h
+++ b/netwerk/protocol/http/nsHttpConnectionInfo.h
@@ -288,6 +288,9 @@ class nsHttpConnectionInfo final : public ARefBase {
 
   static uint64_t GenerateNewWebTransportId();
 
+  uint64_t OriginContextId() const { return mOriginContextId; }
+  void SetOriginContextId(uint64_t aOriginContextId);
+
  private:
   void Init(const nsACString& host, int32_t port, const nsACString& npnToken,
             const nsACString& username, nsProxyInfo* proxyInfo,
@@ -337,6 +340,10 @@ class nsHttpConnectionInfo final : public ARefBase {
   uint64_t mWebTransportId = 0;  // current dedicated Id only used for
                                  // Webtransport, zero means not dedicated
 
+  // vulpine-networklab: isolates the connection pool per browser context.
+  // Zero means "default pool" (backward compatible).
+  uint64_t mOriginContextId{0};
+
   // for RefPtr
   NS_INLINE_DECL_THREADSAFE_REFCOUNTING(nsHttpConnectionInfo, override)
 };
